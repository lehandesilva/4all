"use client";
import classes from "./searchBox.module.css";
//import useDeboucedCallback from 'use-debounce';

export default function SearchBox() {
  //const handleSearch = ;

  return (
    <div className={classes.searchBox}>
      {/* <input className={classes.search} onChange={}/> */}
    </div>
  );
}
